#include "ActiveObject.h"

ActiveObject::ActiveObject(int displayChar, const Position &position, ObjectType type) : Object(displayChar, position,
                                                                                                type) {}
